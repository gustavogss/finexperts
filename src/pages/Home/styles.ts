import { StyleSheet } from "react-native";
const styles = StyleSheet.create({
    container:{
        padding: 20,
    },
    title: {
        color: '#121212',
        fontSize: 20,
        fontWeight:'bold'
    }
})

export default styles;